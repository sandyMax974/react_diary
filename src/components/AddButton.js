import React from "react";
import { Button } from "semantic-ui-react";

const AddButton = () => {
  return (
    <Button
      color="blue"
      icon="add square"
      labelPosition="right"
      content="Add"
    />
  );
};

export default AddButton;
